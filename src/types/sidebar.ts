export type SideBarPropsType = {
  sidebarOpen: boolean;
  setSidebarOpen: React.Dispatch<React.SetStateAction<boolean>>;
};
export type SidebarContainerProps = {
  sidebarOpen: boolean;
};
export type SidebarBackgroundProps = {
  sidebarOpen: boolean;
};
